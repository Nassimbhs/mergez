name: Keep Render Alive

on:
  schedule:
    - cron: '*/10 * * * *'  # every 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render App
        run: |
          echo "Pinging Render service..."
          curl -s https://mergez.onrender.com/api/documents > /dev/null
          echo "✅ Ping sent at $(date)"
